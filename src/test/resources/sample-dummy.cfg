# Valid simple key
api_version=v1

# Valid nested key components
env=dev
endpoint_dev=/service/dev-endpoint

# Unused key (should trigger an info log)
unused_key=some_value

# Invalid resolved key (won't be matched)
endpoint_test=/should/be/ignored
